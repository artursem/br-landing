---
export interface LinkProps {
  label?: string;
  icon?: string;
  href: string;
  blank?: boolean;
}
const { label, icon, href, blank } = Astro.props;
---

<a href={href} class="text-gray-200" target={blank ? "_blank" : "_self"}>
  {icon ? icon : label}
</a>
